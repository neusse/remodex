diff a/android/app/src/main/kotlin/com/remodex/mobile/services/CodexServiceReview.kt b/android/app/src/main/kotlin/com/remodex/mobile/services/CodexServiceReview.kt	(rejected hunks)
@@ -196,7 +196,7 @@ private suspend fun CodexService.markReviewStartAccepted(
 private fun CodexService.bumpThreadActivityAfterReview(threadId: String) {
     val list = _threads.value
     if (list.none { it.id == threadId }) return
-    _threads.value =
+    publishThreads(
         sortThreadsForBridge(
             list.map {
                 if (it.id == threadId) {
@@ -208,5 +208,6 @@ private fun CodexService.bumpThreadActivityAfterReview(threadId: String) {
                     it
                 }
             },
-        )
+        ),
+    )
 }
