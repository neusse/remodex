diff a/android/app/src/main/kotlin/com/remodex/mobile/services/CodexServiceStartThread.kt b/android/app/src/main/kotlin/com/remodex/mobile/services/CodexServiceStartThread.kt	(rejected hunks)
@@ -62,7 +62,7 @@ internal suspend fun CodexService.startThreadInternal(
             throw CodexServiceError.InvalidResponse("thread/start thread decode failed")
         }
     val patched = applyPreferredProjectFallback(decoded, normalizedCwd)
-    _threads.value = upsertThreadRow(_threads.value, patched)
+    publishThreads(upsertThreadRow(_threads.value, patched))
     _activeThreadId.value = patched.id
     resumedThreadIds.add(patched.id)
     return patched
