diff a/android/app/src/main/kotlin/com/remodex/mobile/services/CodexServiceContextWindow.kt b/android/app/src/main/kotlin/com/remodex/mobile/services/CodexServiceContextWindow.kt	(rejected hunks)
@@ -6,6 +6,7 @@ import com.remodex.mobile.core.model.ContextWindowUsageCodec
 import com.remodex.mobile.core.model.JSONValue
 import kotlinx.coroutines.Dispatchers
 import kotlinx.coroutines.withContext
+import kotlinx.coroutines.withTimeout
 
 /**
  * Thread context window via `thread/contextWindow/read`.
@@ -56,7 +57,10 @@ internal suspend fun CodexService.refreshContextWindowUsageInternal(threadId: St
             params["turnId"] = JSONValue.Str(turnId)
         }
 
-        val response = sendRequestImpl("thread/contextWindow/read", JSONValue.Obj(params))
+        val response =
+            withTimeout(12_000L) {
+                sendRequestImpl("thread/contextWindow/read", JSONValue.Obj(params))
+            }
         val resultObject = response.result?.objectValue
         if (resultObject == null) {
             _contextWindowUsageErrorByThread.value =
